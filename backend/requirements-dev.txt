# Locked backend dev dependencies — extends the app lock.
#
# `-r requirements.txt` is the fully-pinned app lock (see #296). The pins below
# add the dev-only tools and their transitive dependencies, at exactly the
# versions CI resolves today, so lint (ruff) and test (pytest) behaviour cannot
# change without a commit here. This closes the second half of #288: with the
# app pinned but these floating, a ruff or pytest release could still alter lint
# or test results in CI with no repository change.
#
# To upgrade a tool: change its pin below to the target version, then
#
#   pip install --dry-run --ignore-installed --report /tmp/dev.json -r requirements-dev.txt
#
# and update the transitive pins to match the report's resolved versions.

-r requirements.txt

# Direct dev tools
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.15.22

# Transitive — pulled in by the tools above, pinned so they cannot drift either
iniconfig==2.3.0
pluggy==1.6.0
pygments==2.20.0
