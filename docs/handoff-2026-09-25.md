# Handoff — 2026-09-25

Recovered after the working copy was lost to an iCloud Drive sync change mid-session (13:53).
This branch (`fix/release-gate-326`) is the in-flight work from that moment, replayed from the session transcript.

## Where things stand

- **`chapter2`** has everything from the autonomous reset run (#317–#347) merged.
  Last green run on `chapter2`: ruff ok, pytest 1079, tsc ok, vitest 518.
- **#326 dependency audit** resolved by #348 (pnpm overrides for browserslist,
  baseline-browser-mapping, js-yaml). `pnpm audit` and `pip-audit` clean.
- `main` untouched.

## In flight on this branch (uncommitted at the time of loss, now committed)

Working through `scripts/local-release.sh` (the full local release gate, #326):

1. Audit step: passes (after #348).
2. Frontend smoke: passes once the Playwright browser binary is installed
   (`pnpm exec playwright install chromium`).
3. **Migration round-trip** was failing: `backend/tests/migration_stable_release_roundtrip.py`
   hardcoded an old Alembic head (`c4a8e2f6b1d9`; real head is `b1d4f6a8c2e9`).
   Fixed: it now reads the head via `ScriptDirectory.from_config(config).get_current_head()`.
4. **New boot guard** `validate_llm_provider_config()` in `backend/app/config.py`,
   called from `backend/app/main.py` next to `validate_origin_config()`:
   refuses to boot with `LLM_PROVIDER=fake` outside development.
   Tests in `backend/tests/test_config_security.py`.

**Not yet verified:** the full `pytest -q` run after these edits was killed (exit 137)
when the folder disappeared. First step next session: run backend tests, then rerun
`scripts/local-release.sh` to the end, then open a PR into `chapter2`.

Note: the release script run moves `backend/.env` / `frontend/.env` to `*.env.aside`
while it runs. Restore them afterwards.

## Engineering review (in progress at time of loss)

Launched at ~13:46: `/code-review` of `chapter2` vs `main`, 10 agents across 5 slices
(platform/shared, autopilot + queue + packets, and others). Reports were written to a
session scratchpad that did not survive. One claim was being verified: *evidence
reject → HTTP 500*. Rerun the review; it is cheap to regenerate.

## Next

1. Finish the release gate (above) → PR into `chapter2`.
2. Rerun the sliced `/code-review` of `chapter2` vs `main`, then `/improve-codebase-architecture`.
3. Open issues: #326 (review + release audit), #319 (umbrella), #208 (owner PostHog deletion).
